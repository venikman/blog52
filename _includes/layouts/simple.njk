<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title or "Site" }}</title>
    <link rel="stylesheet" href="/styles/wiki.css">
  </head>
  <body>
    <header style="padding: 0.75rem 1rem; border-bottom: 1px solid #1f2a37; display: flex; align-items: center; gap: 1rem;">
      <a href="/" style="font-weight: 700; color: inherit; text-decoration: none;">Home</a>
    </header>

    <main class="content content--simple">
      {{ content | safe }}
    </main>
  </body>
</html>
